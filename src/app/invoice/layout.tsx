import "./invoice.css";

export const metadata = {
  title: "Invoice Manager",
  description: "Track and manage your invoices with PDF export",
};

export default function InvoiceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
